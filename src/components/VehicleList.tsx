import { VehicleListContainer } from './VehicleListContainer';

/**
 * Vehicle List Component
 * Re-export of the container component for backward compatibility
 */
export const VehicleList = VehicleListContainer;
export default VehicleList; 