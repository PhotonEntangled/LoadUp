export const dynamic = 'force-dynamic';
import { useForm, Controller, SubmitHandler } from "react-hook-form";
import { auth, UserRole } from '@/lib/auth'; // Assuming auth setup

// Define the form data type
// ... existing code ... 